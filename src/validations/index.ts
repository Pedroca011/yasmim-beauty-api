import { userSchema } from "./user.validator";
import { authSchema } from "./auth.validator";
import { productSchema } from "./product.validator";
import { daySchema } from "./day.validator";

export { userSchema, authSchema, productSchema, daySchema };
